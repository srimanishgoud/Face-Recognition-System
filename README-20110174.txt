Name: S Sri Manish Goud
Roll No: 20110174

Assignment2: README

1) I have implemented this assignment in Jupyter notebook, so it would be better if this file is
   runned in Jupyter notebook 
2) The "Given_Dataset" is the unzipped folder of the given input dataset.
3) Please make sure to install matplotlib, numpy, pandas, scikit_learn, opencv, random
4) The cells should run in a sequential order.
5) "detected_faces" is the folder containing all the detected faces from the given dataset.
6) "labeled_images.csv" contains the images belonging to a person in the labeled format.